# main_action_test.yml in the workflow
name: NodeJS CI Pipeline

on:
  push:
    branches:
      - main

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Setup and Test
        uses: nanjansudhakar/github_actions_library/actions/environment_setup@main
        with:
          node-version: '16'
